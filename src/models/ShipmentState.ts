enum ShipmentState {
    Preparation = "preparation",
    InTransit = "in_transit",
    Delivered = "delivered",
    Returned = "returned",
    Canceled = "canceled",
    Error = "error",
}

export default ShipmentState;
